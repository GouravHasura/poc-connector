import { CapabilitiesResponse } from "@hasura/ndc-sdk-typescript";

export const capabilities: CapabilitiesResponse = {
    version: "^0.1.0",
    capabilities: {
        query: {},
        mutation: {},
    },
}